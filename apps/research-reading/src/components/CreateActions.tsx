export function CreateActions({disabled=false,onNewFolder,onNewDocument,onNewTable,onUpload}:{disabled?:boolean;onNewFolder:()=>void;onNewDocument:()=>void;onNewTable:()=>void;onUpload:()=>void}){return <>{disabled?<button disabled className="button button--primary">新建 ▾</button>:<details className="create-dropdown"><summary className="button button--primary">新建 ▾</summary><div className="create-dropdown-menu">{[['新建在线文件夹',onNewFolder],['新建在线文档',onNewDocument],['新建在线表格',onNewTable]].map(([name,action])=><button key={String(name)} type="button" onClick={event=>{event.currentTarget.closest('details')?.removeAttribute('open');(action as ()=>void)()}}>{String(name)}</button>)}</div></details>}<button disabled={disabled} data-focus-id="research-upload" className="button button--secondary" type="button" onClick={onUpload}>上传</button></>}