export function CreateActions({onNewFolder,onNewDocument,onNewTable,onUpload}:{onNewFolder:()=>void;onNewDocument:()=>void;onNewTable:()=>void;onUpload:()=>void}){return <><details className="create-dropdown"><summary className="button button--primary">新建 ▾</summary><div className="create-dropdown-menu">{[['新建在线文件夹',onNewFolder],['新建在线文档',onNewDocument],['新建在线表格',onNewTable]].map(([name,action])=><button key={String(name)} type="button" onClick={event=>{event.currentTarget.closest('details')?.removeAttribute('open');(action as ()=>void)()}}>{String(name)}</button>)}</div></details><button data-focus-id="research-upload" className="button button--secondary" type="button" onClick={onUpload}>上传</button></>}